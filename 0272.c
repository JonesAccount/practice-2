#include <stdio.h>

int main(void) {
    int size, counter, map;
    size = counter = map = 10;
    
    printf("size: %d\n", size);
    printf("counter: %d\n", size);
    printf("map: %d", size);
    
    return 0;
}