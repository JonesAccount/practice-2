char * pD = &dot;
    bool separator = false;
    
    srand(time(NULL));
    
    while (1) {
        
        
        //for (short i = 0; i < 15; i++)
            //printf("%s ", pD);
            //separator = rand() % 2;
        
        getchar();
        getchar();
    }
}